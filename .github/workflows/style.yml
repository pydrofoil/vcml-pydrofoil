name: style

on: [ push, pull_request]

jobs:
  style:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          submodules: 'recursive'

      - name: Check Code Style
        run: ./sysc_vp/utils/format -n
